DB_HOST=localhost
DB_PORT=5432
DB_NAME=centralwatch
DB_USER=centralwatch
DB_PASSWORD=changeme

DB_POOL_MIN=2
DB_POOL_MAX=10

# Per-environment agent keys, comma-separated env:key pairs.
# Leave blank while you're getting the pipeline working end-to-end;
# fill in before the demo.
AGENT_API_KEYS=dev:changeme-dev-key,staging:changeme-staging-key,prod:changeme-prod-key
